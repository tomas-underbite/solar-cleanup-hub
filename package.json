{
  "name": "kleenup-solar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bunx --bun serve -l 8080 .",
    "build": "rm -rf dist && mkdir -p dist && cp -r *.html robots.txt sitemap.xml 'website assets' 'New HD photos' dist/",
    "start": "bunx --bun serve -l 8080 dist"
  }
}
